import ProfileFiltersContent from './ProfileFiltersContent';

const ProfileFiltersLayout = () => {
  return (
    <div className="hidden flex-col md:flex">
      <ProfileFiltersContent />
    </div>
  );
};

export default ProfileFiltersLayout;
